# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0

# --- Validation & configuration ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
email-validator>=2.1,<3.0
python-multipart>=0.0.9,<1.0

# --- Authentication (Phase 2) ---
bcrypt>=4.1,<5.0
PyJWT>=2.9,<3.0

# --- Database ---
sqlalchemy>=2.0.30,<3.0
psycopg[binary]>=3.2,<4.0
alembic>=1.13,<2.0
pgvector>=0.3,<1.0

# --- Document ingestion (Phase 3) ---
pymupdf>=1.24,<2.0

# --- Embeddings (Phase 3) ---
# torch is installed separately (CPU/GPU wheel) - see Dockerfile and README.
sentence-transformers>=3.0,<5.0

# --- LLM providers (Phase 4) ---
google-genai>=1.0,<2.0
